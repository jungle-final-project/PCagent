export type PartRow = {
  id: string;
  category: string;
  name: string;
  price: number;
  status: string;
  score: number;
};

export type ToolRow = {
  tool: string;
  status: string;
  confidence: string;
  summary: string;
};
